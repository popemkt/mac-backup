# Shim: human shell entry routes through the single gate (intent/gate.sh).
# Registered in intent/SURFACES.md.
use flake
"$PWD"/intent/gate.sh session direnv || log_error "gate: required tools missing — run rebuild or nix develop"
