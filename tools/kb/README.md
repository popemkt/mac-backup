# kb

Repo-native outliner datastore (Bun + TypeScript). See [DESIGN.md](./DESIGN.md).

```bash
bun test
```

M1: model, JsonlStore, DataScript adapter, action registry, `node.*` / `field.define` / `tag.define` / `graph.query`.
