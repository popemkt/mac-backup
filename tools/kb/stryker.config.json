{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Command runner, not vitest: `bun:test` cannot load under vitest unchanged (see t1-property-mutation report). tsconfigFile points at a name that does not exist so Stryker's TSConfigPreprocessor finds nothing to parse — TS 7's native port dropped `ts.parseConfigFileTextToJson`, which that preprocessor calls unconditionally on any tracked tsconfig.json and crashes on.",
  "testRunner": "command",
  "commandRunner": {
    "command": "bun test tests/"
  },
  "coverageAnalysis": "off",
  "tsconfigFile": "stryker-tsconfig-workaround-none.json",
  "mutate": ["src/foundation/**/*.ts"],
  "ignorePatterns": ["ui/**"],
  "reporters": ["clear-text", "progress", "html"],
  "htmlReporter": {
    "fileName": "reports/mutation/index.html"
  },
  "thresholds": {
    "high": 80,
    "low": 60,
    "break": 60
  },
  "timeoutMS": 15000,
  "tempDirName": ".stryker-tmp"
}
